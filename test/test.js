var assert = require("assert");

describe;

describe("register", () => {
  it("should return  http code 200 when trying to register webhook", () => {
    //call localhost:9876/register with body
    /**
     * {
        "url": "http://webhook-endpoint",
        "token" : "foo"
    }
     */

    assert.match("return value", /200/);
  });
});

describe("Execute-all", () => {
  it("should ");
});
